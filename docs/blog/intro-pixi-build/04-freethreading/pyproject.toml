[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "fibtable"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["rich"]

[project.scripts]
fibtable = "fibtable:main"
